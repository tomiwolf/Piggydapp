import Header from "../components/Header";
import HeroFreemint from "../components/HeroFreemint";

export default function Home() {
  return (
    <div className="min-h-screen w-full bg-primary">
      <Header />
      <HeroFreemint />
   
    </div>
  );
}